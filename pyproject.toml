[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "suns-chan"
version = "0.1.0"
description = "Minimal persistent-agent core with inspectable memory and action policy"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
