# Convenience file for `pip install -r requirements.txt` (dev / full install).
# The authoritative dependency specification lives in pyproject.toml.
# Equivalent to:  pip install -e ".[all,dev]"

litellm>=1.40,<2
python-dotenv>=1.0,<2
chromadb>=0.5,<2
python-telegram-bot>=21.0,<23
telegramify-markdown>=1.0,<2
agentmail>=0.5,<1
beautifulsoup4>=4.12,<5

# Dev / test
pytest>=8,<9
ruff>=0.5
